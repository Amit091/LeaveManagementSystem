if typeof errors !== 'undefined'
  each error in errors
    .alert.alert-warning.alert-dismissble.fade.show.shadow.customMsg(role="alert")
      #{error.msg}
      button.close(type="button", data-dismiss="alert", aria-label="Close")
        span(aria-hidden="true") &times

if success_msg !==''
  .alert.alert-success.alert-dismissible.fade.show.shadow.customMsg(role='alert')
  #{success_msg}
  button.close(type='button', data-dismiss='alert', aria-label='Close')
    span(aria-hidden='true') &times;

if warning_msg !==''
  .alert.alert-warning.alert-dismissible.fade.show.shadow.customMsg(role='alert')
  #{warning_msg}
  button.close(type='button', data-dismiss='alert', aria-label='Close')
    span(aria-hidden='true') &times;

if error_msg !==''
  .alert.alert-danger.alert-dismissible.fade.show.shadow.customMsg(role='alert')
  #{error_msg}
  button.close(type='button', data-dismiss='alert', aria-label='Close')
    span(aria-hidden='true') &times;

if error !==''
  .alert.alert-success.alert-dismissible.fade.show.shadow.customMsg(role='alert')
  #{error}
  button.close(type='button', data-dismiss='alert', aria-label='Close')
    span(aria-hidden='true') &times;

block script
  script
    